package Days;

import java.time.LocalDateTime;

public class YearlyObj extends MyObject{
    public YearlyObj(String title, String description, String tasktype, LocalDateTime firstDate, Integer counter, Integer id) {
        super(title, description, tasktype, firstDate, counter, id);
    }
}
