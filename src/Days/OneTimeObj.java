package Days;

import java.time.LocalDateTime;

public class OneTimeObj extends MyObject {

    public OneTimeObj(String title, String description, String tasktype, LocalDateTime firstDate, Integer counter, Integer id) {
        super(title, description, tasktype, firstDate, counter, id);
    }
}
